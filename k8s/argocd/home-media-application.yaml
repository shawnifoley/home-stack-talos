apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: home-media
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  source:
    repoURL: https://github.com/shawnifoley/home-stack-talos.git
    targetRevision: master
    path: k8s/media-stack
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
