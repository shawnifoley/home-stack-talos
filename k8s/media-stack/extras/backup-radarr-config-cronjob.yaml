apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-radarr-config
  namespace: media
spec:
  schedule: "30 2 * * 0"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      template:
        metadata:
          labels:
            app: backup-radarr-config
        spec:
          restartPolicy: OnFailure

          # Force the backup pod to run on the same node as the radarr pod
          affinity:
            podAffinity:
              preferredDuringSchedulingIgnoredDuringExecution:
                - weight: 100
                  podAffinityTerm:
                    labelSelector:
                      matchLabels:
                        app: radarr
                    topologyKey: kubernetes.io/hostname

          containers:
            - name: backup
              image: alpine:3.20
              imagePullPolicy: IfNotPresent
              command:
                - /bin/sh
                - -ceu
                - |
                  apk add --no-cache rsync >/dev/null
                  root="/backup/backups/radarr-config"
                  dest="${root}/Backups"
                  mkdir -p "${dest}"

                  if [ -d /config/Backups ]; then
                    rsync -azP --delete /config/Backups/ "${dest}/"
                  else
                    echo "No /config/Backups directory found; nothing to sync."
                  fi
              volumeMounts:
                - name: source
                  mountPath: /config
                  readOnly: true
                - name: backup
                  mountPath: /backup
          volumes:
            - name: source
              persistentVolumeClaim:
                claimName: radarr-config
            - name: backup
              persistentVolumeClaim:
                claimName: nfs-tank-root
