apiVersion: batch/v1
kind: CronJob
metadata:
  name: restart-vaultwarden
  namespace: media
spec:
  schedule: "10 3 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: kubectl
              image: bitnami/kubectl
              command:
                - kubectl
                - rollout
                - restart
                - deployment/vaultwarden
          restartPolicy: OnFailure
