apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-tank-root
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 1Ti
  volumeName: nfs-tank-root
